[project]
name = "ksa-ev-tender-monitor"
version = "1.0.0"
requires-python = ">=3.10"
description = "Daily monitoring of KSA EV-related tenders across multiple sources"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py310"
